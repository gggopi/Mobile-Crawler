1.compile the java file
   javac KO.java

2.place the class file (KO.java) from where aglets gets executed

3.open 2 tahiti servers (client and remote site) from bin folder:
  ./agletsd -f ../cnf/aglets.props
  ./agletsd -f ../cnf/agleds.props -port 8000

4.run the code in tahiti.
   create --> dispatch --> retract

for other things,
please refer project report..
